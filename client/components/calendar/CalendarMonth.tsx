/*const CalendarMonth = ()=>{
  return(
    {/* 1. Render Placeholders to push the 1st to the right column */}
    /*  {[...Array(data)].map((_, i) => (
        <div key={`blank-${i}`} className="hidden sm:block sm:basis-[calc(100%/7)] sm:aspect-square border-r border-b bg-gray-50" />
      ))}

      {/* 2. Render Actual Days */}
    /*  {calendarPrices.map(([date, rooms]) => (
        <div key={date} className="basis-full sm:basis-[calc(100%/7)] aspect-[4/1] sm:aspect-square border-r border-b">
         DAY
        </div>
      ))}
  )
}*/

export const CalendarMonth = ()=>{
return(
    <div></div>
    
  )
}

